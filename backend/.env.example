# AI Provider Configuration
# Note: Local Ollama (deepseek-r1:1.5b) is used by default and does not require API keys
# These keys are only used when local processing fails

# DeepSeek API (First Fallback)
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# Claude API (Second Fallback)
CLAUDE_API_KEY=your_claude_api_key_here

# Server Configuration
PORT=8000
HOST=0.0.0.0

# File Upload Configuration
MAX_FILE_SIZE=5242880  # 5MB in bytes
